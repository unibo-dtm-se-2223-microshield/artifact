services:
  dashield-supervisor:
    build:
      context: ./supervisor
      dockerfile: Dockerfile
    container_name: dashield_supervisor
    ports:
      - "8050:8050"
    environment:
      - DASH_HOST=0.0.0.0
      - DASH_PORT=8050
      - DASH_DEBUG=false
    restart: unless-stopped
